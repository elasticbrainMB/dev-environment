# Scoring rubric — job-listing fit

**Status: not yet extracted.** Per Matt's 2026-09-12 instruction, this file
is meant to hold the fit-scoring rubric exactly as it currently lives inside
the "Daily job search 2026 v2" Cowork scheduled task's prompt — verbatim, not
cleaned up or improved on the way out. Matt reviews and edits this file
before anything scores against it; nobody should tighten the wording on the
way in.

**Blocked on access, not on effort.** Cowork rewrites a scheduled task's
prompt after its first run, so the current live version only exists in
Cowork's own Scheduled sidebar — a surface this Claude Code session has no
tool access to. Reconstructing it from memory or from an earlier
conversation would risk quietly improving or misremembering it, which is
exactly what Matt asked not to happen. So this stays a stub until he pastes
in the live prompt text (or the fit-scoring portion of it), at which point
it gets extracted here exactly as given, unedited, and Matt takes it from
there.
